---
import Header from '@/components/layout/Header.astro';
import Footer from '@/components/layout/Footer.astro';
import '../styles/global.css';
import '../styles/marketing-overrides.css';

interface Props {
  title: string;
  description: string;
  pageKey?: string;
  ogImage?: string;
}

const { title, description, pageKey = 'crm', ogImage = '/assets/images/og-default.jpg' } = Astro.props;
const canonicalURL = new URL(Astro.url.pathname, Astro.site);
const ogImageURL = new URL(ogImage, Astro.site);
---
<!DOCTYPE html>
<html lang="en" class="scroll-smooth">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="generator" content={Astro.generator} />

    <!-- SEO -->
    <title>{title}</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={canonicalURL} />

    <!-- Open Graph -->
    <meta property="og:type" content="website" />
    <meta property="og:site_name" content="SyncAgency" />
    <meta property="og:locale" content="en_US" />
    <meta property="og:url" content={canonicalURL} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={ogImageURL} />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />

    <!-- Twitter Card -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:site" content="@syncagency" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={ogImageURL} />

    <!-- Favicon + manifest -->
    <link rel="icon" href="/favicon.ico" sizes="any" />
    <link rel="icon" type="image/png" sizes="32x32" href="/assets/images/favicon-32x32.png" />
    <link rel="apple-touch-icon" href="/assets/images/logo-original.png" />
    <link rel="manifest" href="/manifest.json" />

    <!-- Lucide icons (local — stable cache) -->
    <script src="/assets/scripts/lucide.min.js" defer></script>

    <!-- Page-specific head (JSON-LD schemas, etc.) -->
    <slot name="head" />

    <!-- Organization JSON-LD -->
    <script type="application/ld+json" set:html={JSON.stringify({
      "@context": "https://schema.org",
      "@type": "Organization",
      "@id": "https://syncagency-en.vercel.app/#organization",
      "name": "SyncAgency",
      "alternateName": ["Sync Agency", "SyncAgency CRM"],
      "legalName": "SyncAgency",
      "url": "https://syncagency-en.vercel.app/",
      "logo": {
        "@type": "ImageObject",
        "url": "https://syncagency-en.vercel.app/assets/images/logo-original.webp",
        "width": 240,
        "height": 240
      },
      "description": "The CRM for creator talent agencies managing Telegram and WhatsApp conversations, tracking operators, and attributing revenue across creators.",
      "foundingDate": "2026",
      "founders": [
        {
          "@type": "Person",
          "name": "Lucas Provenzano",
          "url": "https://www.linkedin.com/in/lucas-provenzanooo/"
        }
      ],
      "address": {
        "@type": "PostalAddress",
        "addressLocality": "Gerpinnes",
        "addressCountry": "BE"
      },
      "knowsAbout": [
        "Creator talent agency software",
        "Telegram CRM",
        "WhatsApp CRM",
        "Creator economy SaaS",
        "Operator management",
        "Broadcast campaign automation",
        "Social media analytics",
        "Marketing attribution",
        "Acquisition tracking",
        "Subscription analytics",
        "Revenue attribution"
      ],
      "knowsLanguage": ["en", "fr"],
      "sameAs": [
        "https://t.me/syncagency",
        "https://www.instagram.com/syncagency"
      ],
      "contactPoint": {
        "@type": "ContactPoint",
        "contactType": "customer support",
        "email": "contact@syncagency.fr",
        "availableLanguage": ["English", "French"]
      },
      "areaServed": {
        "@type": "Place",
        "name": "Worldwide"
      }
    })} />
  </head>
  <body>
    <a href="#main-content" class="skip-link">Skip to main content</a>
    <Header pageKey={pageKey} />
    <main id="main-content">
      <slot />
    </main>
    <Footer pageKey={pageKey} />

    <!-- Scripts -->
    <script src="/scripts/header-scroll.js" defer></script>
    <script src="/scripts/mobile-menu.js" defer></script>
    <script src="/scripts/accordion.js" defer></script>
    <script src="/scripts/fade-in-scroll.js" defer></script>
    <script src="/scripts/spotlight.js" defer></script>
    <script src="/scripts/pivot-reveal.js" defer></script>
    <script src="/scripts/video-toggle.js" defer></script>
    <!-- Chat widget SAV -->
    <div class="chat-widget" id="chatWidget" aria-live="polite">
      <div class="chat-panel" id="chatPanel" role="dialog" aria-label="SyncAgency Support" aria-hidden="true">
        <div class="chat-panel-header">
          <div class="chat-panel-identity">
            <div class="chat-panel-avatar">
              <img src="/assets/images/lucas-avatar.jpg" alt="Lucas" />
            </div>
            <div>
              <p class="chat-panel-name">Lucas</p>
              <span class="chat-panel-status"><span class="chat-status-dot"></span>Online</span>
            </div>
          </div>
          <button class="chat-panel-close" id="chatClose" aria-label="Close">
            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg>
          </button>
        </div>
        <div class="chat-panel-body">
          <p class="chat-greeting">Hey there 👋</p>
          <p class="chat-message">Questions about SyncAgency? We reply fast on Telegram.</p>
          <a href="https://taap.it/pvzlucas" class="chat-tg-btn" target="_blank" rel="noopener">
            <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="m22 2-7 20-4-9-9-4Z"/><path d="M22 2 11 13"/></svg>
            Open Telegram
          </a>
          <p class="chat-hint">Response within 24h</p>
        </div>
      </div>
      <button class="chat-bubble" id="chatBubble" aria-label="Open support" aria-expanded="false">
        <svg class="chat-icon-msg" xmlns="http://www.w3.org/2000/svg" width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/></svg>
        <svg class="chat-icon-close" xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg>
      </button>
    </div>

    <script>
      document.addEventListener('DOMContentLoaded', function() {
        if (window.lucide) lucide.createIcons();

        // Chat widget SAV
        (function() {
          var widget   = document.getElementById('chatWidget');
          var bubble   = document.getElementById('chatBubble');
          var panel    = document.getElementById('chatPanel');
          var closeBtn = document.getElementById('chatClose');
          if (!widget || !bubble || !panel) return;
          function openChat()  { widget.classList.add('is-open'); bubble.setAttribute('aria-expanded','true'); panel.setAttribute('aria-hidden','false'); }
          function closeChat() { widget.classList.remove('is-open'); bubble.setAttribute('aria-expanded','false'); panel.setAttribute('aria-hidden','true'); }
          bubble.addEventListener('click', function() { widget.classList.contains('is-open') ? closeChat() : openChat(); });
          if (closeBtn) closeBtn.addEventListener('click', closeChat);
          document.addEventListener('keydown', function(e) { if (e.key === 'Escape') closeChat(); });
        })();

        // CTA tracking
        document.querySelectorAll('[data-cta]').forEach(function(btn) {
          btn.addEventListener('click', function() {
            var cta = btn.getAttribute('data-cta');
            var section = btn.getAttribute('data-section');
            try {
              if (window.va) {
                window.va('event', { name: 'cta_clicked', data: { cta: cta, section: section } });
              }
            } catch(e) {}
          });
        });
      });
    </script>
  </body>
</html>
